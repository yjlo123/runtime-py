names = ['siwei', 123]  // define an array

if names.len > 3 {
    print(1 + names.len)
    print('long')
}

func say_hi(name) {
    return 'hi ' + name
}

for name : names {
    print(say_hi(name))
}

/*
class Counter {
    name = nil
    count = 0

    Counter(name) {
        this.name = name
    }

    func increase() {
        this.count++
    }

    func to_str() {
        return this.name + ' ' + this.count
    }
}

c1 = Counter('test')
c1.increase()
c1.increase()
print(c1.to_str())
*/
